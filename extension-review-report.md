# Chrome Web Store Review Report

**Extension**: Persona.chat
**Version**: 0.4.0
**Scanned**: 2026-08-09
**Verdict**: LIKELY APPROVED

## Summary

| Severity | Count |
|----------|-------|
| CRITICAL | 0     |
| HIGH     | 0     |
| MEDIUM   | 0     |
| PASS     | 12    |

---

## Scope of this pass

This release's diff vs 0.3.0: two new bilingual personas (`seed_work_stock_research`,
`seed_work_domain_research`, content-only, no new code paths), a store-listing copy
rewrite (Dashboard-only, not shipped in the package), and a new feedback mechanism
(`chrome.runtime.setUninstallURL` on uninstall + two outbound `target="_blank"` links
in the popup and options page pointing at an independently-hosted Cloudflare Pages
site). No manifest changes.

## Issues Found

None.

## Passed Checks

- [x] No remote code execution (`eval`, `new Function`, remote `<script src>`, dynamic `import()` from a URL) — zero matches
- [x] No code obfuscation in extension logic — one pre-existing, justified `atob()` call (`src/lib/character-card-import.ts:85`, decodes the base64 PNG tEXt chunk of a user-imported SillyTavern/chub.ai character card; user-provided file content, not hidden extension code)
- [x] Permissions unchanged and still minimal: `storage`, `unlimitedStorage`, `tts` + 3 host permissions (chat.deepseek.com, claude.ai, chatgpt.com) — the new feedback feature adds **zero** new permissions, confirmed by diffing `build/chrome-mv3-prod/manifest.json` against 0.3.0
- [x] Privacy policy present (`PRIVACY.md`, already registered in the CWS Developer Dashboard from initial submission)
- [x] Zero `fetch`/`XMLHttpRequest`/`axios` calls anywhere in `src/` — the feedback mechanism is redirect-only (`setUninstallURL`) and link-only (`target="_blank"`), never a network call made by the extension itself
- [x] No insecure (`http://`) transmission — the one `http://` string match is an XML namespace identifier inside an inline SVG (`src/lib/backgrounds.ts:33`), not a fetched URL
- [x] No `chrome.history`/`chrome.webNavigation`/tab-URL tracking
- [x] No crypto-mining or copyright-circumvention patterns
- [x] Single purpose intact — feedback/uninstall-survey is part of the same product's user-input loop, not an unrelated bolted-on feature
- [x] Minimum functionality — well beyond a link-only extension (101+ personas, world info, adapters for 3 platforms, options UI)
- [x] Outbound links are disclosed in context — the chub.ai link's tooltip states it's a third-party site "not affiliated with us"; the feedback link is plainly labeled
- [x] Manifest metadata complete (name/description/icons/version), description resolves via `chrome.i18n` (`__MSG_extensionDescription__`), so store-listing copy stays in sync with the manifest automatically — no separate manual-sync step needed this release

## Next Steps

1. No fixes required.
2. Proceed to packaging + upload.

---

*Generated by extension-review skill. Reference: https://developer.chrome.com/docs/webstore/troubleshooting*
