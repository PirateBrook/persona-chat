/**
 * Service worker entry. Kept intentionally empty in v0: no cross-origin
 * fetches, no license verification, no scheduled jobs. Add here when we need
 * badge counts, context-menu items, or extension messaging.
 */
export {}
