# 更新日志

## [未发布]

### 商店文案发现性（Workstream C）
- 商店短描述/详细描述整体改写：之前的文案（"a bar owner, a talking cat"）领着"角色扮演/陪伴平台"这个 7/30 战略收敛里明确退役的定位在讲；改成领着"有目的的人设"（练面试/练谈判/研究）+ 信任信号（无需 API Key/不上传），同步进 `locales/{en,zh_CN}/messages.json` 的 `extensionDescription`（新英文文案 122 字符，仍在 CWS 132 字符上限内）
- CWS 分类给出待人工确认的建议：从"Fun"改成"Productivity"——理由同上，分类是实际改变商店可见状态的 Dashboard 操作，本轮只写建议不代自动执行
- 现有商店截图仍是旧的角色扮演在前的取景，本轮未重拍，留给下次提审前处理

### 人设质量审计（Workstream A 试点）
- 对照 `docs/strategy.md` §1 的 5 个真实用例逐一核对内置人设库：练谈判/练面试两类已有人设（薪资谈判教练、谈判陪练对手、行为面试特训）本身就是"开场先反问关键上下文"的写法，无需改写；调研股票、研究领域两类完全没有对应人设，属于真实空白，补了 `seed_work_stock_research`（股票研究搭子）和 `seed_work_domain_research`（新领域研究向导）两个双语人设，同样走"先反问、给结构、不编数据"的模式——股票搭子明确声明无实时行情、绝不编造具体股价/财报数字；研究向导产出"子话题地图"而非一次性长篇讲课，一次只深入用户挑的那一支
- 约会告白演练场景经评估后判定不适合做（贴 CWS 对"AI 恋爱陪伴"类的监管风险、也偏离"零深度情感陪伴"的冻结清单），从这轮缺口里剔除，不补

### 零成本长会话记忆（Stage 2 方向 2，A-only）
- 记忆便签升级为"AI 自摘要记忆"：📌 记忆卡片内新增"🧠 让 AI 总结"——一键把 locale-aware 的"把目前剧情/设定/关系概括成 5 条要点"提示注入输入框（可见注入，人工按 Enter，用**你自己那次免费对话**的额度让模型总结它自己的上下文，插件不调任何 API），和"📥 用上一条回复"——把模型回复的摘要拉进记忆草稿供审阅后保存为常驻记忆（每次 ✨ 增强自动重注入）。零成本、零新权限、零自发网络请求
- 机制说明：这是简化版酒馆的 SupaMemory（自动摘要记忆）在"零 LLM 成本 + 可见注入"两条硬约束下的合规改造——不照抄付费 summarizer/向量库，改由用户自己的对话自摘要（模型持有自己的上下文，绕开插件读不到 virtualized 历史的限制）
- options 人设编辑器新增记忆用量显示："记忆用量：N 条 · 约 X 字"，超预算（4000 字）变琥珀警告色并提示合并/删除——记忆是 `alwaysActive` 每次都注入，避免无界膨胀拖慢存储/稀释上下文（仅提示，绝不自动删用户数据）

### 易用性（浮动按钮排版 + 使用说明）
- 页面上的浮动操作按钮从"三个带文字的宽 pill 横排"改为"图标钮竖排在主按钮上方"——只占屏幕最右一列，不再横向伸进对话框/发送键区域（此前在真实对话里会挡住输入区）。每个图标 hover 显示说明气泡
- 首次激活角色时弹一次引导提示："先打字 → 点 ✨ 把角色和记忆织进去 → 自己按 Enter"，讲清"插件从不自动发送、由你按回车"这个反直觉的核心机制；点"知道了"后永久不再出现
- 设置页新增"使用说明"区块（双语，默认展开）：一句话讲清插件是什么 + ✨ 增强 / 🎭 守卫 / 📌 记忆三个按钮各自的作用 + 使用流程
- "你的名字"输入框去掉开发者术语 `{{user}}`，标签改为普通的"你的名字"并加一行说明"角色在故事里会这样称呼你"

### 修复
- 修复浮动操作按钮的布局碰撞：🎭 反串扮守卫 与 📌 记忆两个 pill 此前都硬编码在 `right-56`、相互重叠（中文标签更宽时尤其明显，是 locale 相关的定位 bug）；改为竖排图标栈后彻底消除（图标钮无标签宽度差异，天然不受语言影响）

## [0.3.0] - 2026-07-28

### 商店列表国际化基础设施
- 扩展包补上标准 `chrome.i18n` 国际化：新增 `locales/en/messages.json`、`locales/zh_CN/messages.json`（`extensionName`/`extensionDescription` 两个 key），`package.json` 顶层 `displayName`/`description` 改用 `__MSG_extensionName__`/`__MSG_extensionDescription__` 占位符，`manifest.default_locale` 设为 `en`
- 动机：Chrome Web Store Developer Dashboard 的商店列表语言下拉框此前锁死在单一语言——根因是扩展包从未做过 manifest 级别的 i18n（无 `_locales` 目录），Dashboard 检测不到多语言支持。补上这层之后才能在 Dashboard 里为中文单独填一份独立的详细描述/截图
- 与应用内 UI 现有的 `src/lib/i18n/{en,zh}.ts` + `useI18n()` 纯 TS 字典机制完全独立，互不影响；这次改动只影响 `chrome://extensions` 显示的扩展名/描述和 CWS 商店列表的语言解锁，不改任何应用内文案
- 实测确认 Plasmo 0.90.5 无需额外的 `manifest.name` 覆盖字段——仅改 `displayName`/`description`/`default_locale` 三处，构建产物 `manifest.json` 里 `name`/`description` 就已正确保留字面量 `__MSG_*__` 占位符（Chrome 运行时解析，非构建时替换）

## [0.2.0] - 2026-07-28

### 平台扩展
- ChatGPT 成为第三个支持平台：人设/世界书/背景全套功能通过新的 `PlatformAdapter` 同时运行在 chatgpt.com 上，零 LLM 成本，与 DeepSeek/Claude.ai 一样是"读草稿→写回→人工按发送"的可见注入机制；manifest 只多了一个 `host_permissions`，无新增 API 权限

### 对话级注入控制（方向 5 第一批）
- 人设文本支持 `{{char}}`/`{{user}}`/`{{random:a,b,c}}` 宏，注入时展开为真实人设名/用户称呼/随机选项——修复了导入的 SillyTavern/chub.ai 角色卡若使用这套通用占位符写法、原样显示花括号 token 的潜在问题
- 新增"🎭 反串扮守卫"按钮：一键在草稿上方注入"不要替我说话/行动"的提醒（双语，宏自动展开），与"✨ 增强"同样是可见注入、不自动发送
- 设置里新增"你的名字"（全局，用于 `{{user}}` 宏展开，留空有语言相关的默认兜底）

### 编辑页排版重做
- 人设列表（现有 100+ 内置角色）加了搜索框 + 标签筛选，列表和编辑器区域各自独立滚动，不再是整页随人设数量无限变长
- 列表卡片改为整行可点击进编辑，复制/删除收进悬浮显现的图标按钮；标出当前生效人设
- 顶部图标从占位紫色圆点换成实际的熊猫吉祥物图标（options 页、popup、DeepSeek/Claude.ai 面板头统一）

### 修复
- 修复一个自我永续的存储写循环：2 个内置人设（塔罗牌占卜师、无常主题哲学人设）缺少 `backgroundId` 字段，导致每次人设列表刷新都误判"内容变了"并重写 storage，进而不断触发下一次刷新——表现为人设面板打开后列表反复闪动/重排（尤其在 pin 一个角色后滚动列表时明显）
- 修复生产构建（Parcel）会把"裸 emoji 紧跟模板字面量插值"（如 `` `🎭 ${x}` ``）截断成残缺字符的问题，影响反串扮守卫按钮和已上线的漂移提醒、人设列表分组标题
- 修复宏展开（`{{char}}`/`{{user}}`/`{{random}}`）的自我污染问题：人设名或用户名若恰好包含字面量宏 token，会被后续展开步骤二次处理；`{{random:...}}` 也改成按人设确定性选取，修复了同一条常驻世界书设定在激活消息和"增强"里各自独立随机、可能自相矛盾的问题
- 修复反串扮守卫按钮在注入失败时没有任何提示的问题；修复用户名跨标签页修改后未同步到已打开面板/编辑页的问题；修复用户名输入框连续快速输入时可能因存储读写乱序而丢字的问题；补回编辑页排版重做时遗漏的键盘可访问性（删除按钮 focus 时不可见）和世界书条目数提示

## [0.1.0] - 2026-07-24

### 平台扩展
- Claude.ai 成为第二个支持平台：人设/世界书/背景/TTS 全套功能通过新的 `PlatformAdapter` 同时运行在 claude.ai 上，零 LLM 成本，与 DeepSeek 上一样是"读草稿→写回→人工按发送"的可见注入机制；manifest 只多了一个 `host_permissions`，无新增 API 权限

### 世界书 V3 装饰器
- 世界设定从纯关键词匹配升级为 Character Card V3 装饰器模型：`position`（`constant` 条目折叠进一次性激活消息）/ `depth`+`order`（Enrich 消息内排序）/ `role`（📖/⚙️/🎭 可见框架标签）；导入 SillyTavern V3 角色卡时识别这些字段
- （安全考量）`@@use_regex` 未采用：卡片可自定义的正则在页面主线程同步执行，`(a+)+$` 这类模式会灾难性回溯冻结标签页，权衡后关键词匹配保持固定字面量，不支持正则

### 人设面板易用性
- 每个人设可携带专属场景背景图，随人设一起导出/导入
- 置顶 + 最近使用排序（📌 置顶 / 🕒 最近使用 / 全部 分组），应用人设时记录 `lastUsedAt`（不影响 `updatedAt`，不算编辑）
- 搜索框回车直接应用第一个结果；标签筛选跨面板重开保持记忆
- 支持一键克隆/复制人设
- 面板绑定视口高度、内部滚动，小屏不会把标题/tab 挤出去；面板字号整体上调一档

### 内置人设库
- 精简重组（100 → 101）：砍掉 19 个吸引力偏弱的怀旧/搞笑类人设，换成 20 个更贴合英语市场的实用类（职场文书类 8 个、图像生成提示词助手 6 个、创作辅助 6 个）

### 视觉
- 图标改版：从占位字母图标换成熊猫吉祥物图标，延续原有紫色渐变品牌配色

### 修复
- 世界书常量条目折叠进激活消息后，曾经被误判为"已处理"从而在 Enrich 里不再触发，导致常驻设定在新对话/刷新后失效；现修正为折叠与常驻触发是叠加关系，互不影响
- 人设列表在某条数据缺失 `tags` 字段时会整体崩溃白屏（`TypeError: n.tags is not iterable`）；修复为对缺失的 `tags` 做兜底，避免单条脏数据拖垮整个列表

### 发布前验证（本次会话）
- Claude.ai 人设注入/背景、世界书 V3 装饰器（折叠/排序/标签/常驻叠加）、置顶最近排序、每人设专属背景、克隆人设：均已用 `browser-cdp` 真机自动化验证通过（穿透 Plasmo shadow root 驱动真实加载的扩展，非仅静态审查）
- 新增的 `https://claude.ai/*` host 权限已过 `extension-review` 复扫，0 Critical/High/Medium

## [0.0.1] - 2026-07-22 - 首次提交 Chrome Web Store 审核

### 核心角色扮演功能
- 人设卡：性格、场景（scenario）、示例对话、开场白；可视化面板一键应用到 chat.deepseek.com 输入框
- 世界设定（World Info）：关键词触发的背景知识，"✨ 增强"按钮匹配草稿内容后织入消息
- 防跑偏提醒：长对话中每隔几条消息重新插入一次角色提醒
- 背景系统：12 款手工调校的 CSS 背景（渐变 + SVG 噪点 + 点缀元素），支持上传自定义图片
- DeepSeek 页面定制：隐藏"已思考"折叠块等开关（可扩展的"定制" tab）

### 内容与生态
- 内置人设从 15 个扩充到 100 个（陪伴/角色扮演/工作/娱乐/哲学五大类），中英文均为人工撰写/文化重设计，非机翻
- 支持导入 SillyTavern/chub.ai 角色卡（PNG tEXt chunk 或 JSON，V2/V3 格式）
- 用户可打点📌记忆便签，自动转为常驻世界设定条目
- TTS 朗读回复（chrome.tts，本地语音引擎，含音色选择）

### 平台与本地化
- 中英双语 UI 与内容，根据浏览器语言自动检测（`AppState.language: auto/en/zh`）
- 全程零服务器架构：所有数据存在 `chrome.storage.local`，无网络请求、无遥测、无第三方 SDK

### 上架准备（本次会话）
- 修复依赖漏洞：`pnpm.overrides` 修复 msgpackr/content-security-policy-parser 两个 High 漏洞，postcss 直接升级；`@parcel/reporter-dev-server` 因会破坏 Parcel 兼容性检查，评估后有意保留不动（仅影响 `pnpm dev`，不进最终包）
- 完善 `PRIVACY.md` 联系邮箱（改为可实际收信的 `punkscosmos@gmail.com`）
- 补充 `tts` 权限的 CWS 提交理由说明（`docs/chrome-web-store-listing.md`）
- 通过 `extension-review`/`extension-analyze` 两轮 CWS 合规与安全审计，0 个 Critical/High/Medium 发现
- 完成 Chrome Web Store 开发者账号注册、非交易者身份声明、联系邮箱验证
- 提交审核：分类 Fun，中文列表优先上线，英文列表计划审核通过后补充

---

*格式参考 [Keep a Changelog](https://keepachangelog.com/)，版本号暂未启用严格语义化（发布审核通过后再考虑升到 0.1.0）。*
